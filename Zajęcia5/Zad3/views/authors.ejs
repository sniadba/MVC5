<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>authors</title>
</head>
<body>
    <%- include('partials/navigation') %>
<h1>Authors</h1>
<ul>
    <% if (authors.length > 0) { %>
        <% authors.forEach(author => { %>
            <li><%= author.name %></li>
        <% }) %>
    <% } else { %>
        <li>No authors has been found</li>
    <% } %>
</ul>
</body>
</html>